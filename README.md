# MetaShield

A metadata reader and editor tool focused on privacy, cybersecurity, and digital forensics. Supports images (JPG, PNG), PDFs, and audio (MP3).

## Installation

1. Clone or create the project folder structure as shown.
2. Install the required Python libraries:

```bash
pip install pillow piexif pypdf2 mutagen
```
this is try
